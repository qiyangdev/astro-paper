# Public Umami instance configuration defined in `astro.config.ts`.
UMAMI_URL=https://cloud.umami.is

# Required by the server-side `/api/online` Vercel Function.
UMAMI_API_TOKEN=

# Required by both the tracker and the online visitor counter.
UMAMI_WEBSITE_ID=
